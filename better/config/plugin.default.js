const path = require('path');

module.exports = {
  'better-router': {
    enable: true,
    path: path.resolve(__dirname, '../plugins/better-router')
  },
  'better-views': {
    enable: true,
    path: path.resolve(__dirname, '../plugins/better-views')
  },
}
